interface:
  display_name: "Delegate Implementation"
  short_description: "Delegate implementation while keeping discussion active"
  default_prompt: "Use $delegate-work-in-stages to complete this implementation with subagents while you handle implementation decisions and keep this conversation available for discussion."

policy:
  allow_implicit_invocation: false
