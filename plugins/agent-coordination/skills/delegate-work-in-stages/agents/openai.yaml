interface:
  display_name: "Delegate Work in Stages"
  short_description: "Delegate staged implementation while keeping chat active"
  default_prompt: "Use $delegate-work-in-stages to delegate implementation in stages while keeping this chat available for discussion."

policy:
  allow_implicit_invocation: false
